/** Copyright 2016 MarFil Studios. All rights reserved. **/

#include "3DAsset.h"

using namespace std;

void Asset3D::convertMesh(int meshNo) {
	//delete mesh;
	//mesh = new mMesh();
	//for (unsigned int i = 0; i < scene->mMeshes[meshNo]->mNumVertices) {
	//	mVertex vertex;
	//	vertex.Position.x = scene->mMeshes[meshNo]->mVertices[i].x;
	//	vertex.Position.y = scene->mMeshes[meshNo]->mVertices[i].y;
	//	vertex.Position.z = scene->mMeshes[meshNo]->mVertices[i].z;
	//	mesh->vertices.insert(mesh->vertices.end, vertex);
	//}
	//for (unsigned int i = 0; i < scene->mMeshes[meshNo]->mNumFaces; i++) {
	//	scene->mMeshes[meshNo]->mFaces[i].mIndices
	//}
}